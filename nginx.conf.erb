events {}

http {
  access_log stdout;
  server {
  listen {{port}};

    location / {
    <% if ENV['FEELING'] == "happy" %>
        return 200 'happy happy world!';
    <% else %>
        return 200 'nooooooooo!';
    <% end %>
    }
  }
}
